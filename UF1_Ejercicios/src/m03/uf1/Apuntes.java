package m03.uf1;

public class Apuntes {

    public static void main(String[] args) {

        /*
        
        FUNCIONES STRINGS ->
        
        String test = "Esto es un test";
        
        test = test.replace('ç', 'c');
        
        if (test.contains("á")) {
            test = test.replace('á', 'a');
        }
        
        System.out.printf("Caràcter a la posició : %s%n"
        , test.charAt(10));
        
        SABER SI ENTRA UNA LETRA ->
        
        if(isLetter(frase.charAt(5)))
        
        PARSEINT ->

	+También vale con double, float etc...
        
        String serieNumeros = in.nextLine();
        String [] numeros = serieNumeros.split(" ");
        int [] n = new int [numeros.length];
        
        for (int i = 0; i < numeros.length; i++) {
            
            n[i] = Integer.parseInt(numeros[i]);
        }
        
        
         */
    }
}
